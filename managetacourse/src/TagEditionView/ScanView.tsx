import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

export default function ScanTagView() {
  return (
    <View>
      <Text>This is the Scan Tag View</Text>
    </View>
  );
}
