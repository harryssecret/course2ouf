{% extends 'base.html.twig' %} {% block title %}Ranking index{% endblock %} {%
block body %}
<h1>Ranking index</h1>

<br>
<div class="flex gap-4"
    <a href="/rankBy=vitesse">Trier par vitesse croissante</a>
    <a href="/rankBy=lent">Trier par vitesse décroissante</a>
    <a href="/rankBy=men">Trier les Hommes</a>
    <a href="/rankBy=women">Trier les Femmes</a>
    <a href="/rankBy=grade">Trier en fonction des classes</a>
</div>
<br>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Endrun</th>
            <th>Coureur</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for ranking in rankings %}
        <tr>
            <td>{{ ranking.id }}</td>
            <td>{{ ranking.endrun ? ranking.endrun|date('H:i:s') : '' }}</td>
            <td>
                {{ ranking.Student }}
            </td>
            <td>
                <a href="{{ path('app_ranking_show', { id: ranking.id }) }}"
                    >show</a
                >
                <a href="{{ path('app_ranking_edit', { id: ranking.id }) }}"
                    >edit</a
                >
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="3">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<a href="{{ path('app_ranking_new') }}">Create new</a>
{% endblock %}
