{% extends 'base.html.twig' %}

{% block title %}Ranking{% endblock %}

{% block body %}
    <h1>Ranking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ranking.id }}</td>
            </tr>
            <tr>
                <th>Endrun</th>
                <td>{{ ranking.endrun ? ranking.endrun|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ranking_index') }}">back to list</a>

    <a href="{{ path('app_ranking_edit', {'id': ranking.id}) }}">edit</a>

    {{ include('ranking/_delete_form.html.twig') }}
{% endblock %}
